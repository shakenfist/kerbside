libmariadb-dev-compat [platform:debian]
libmysqlclient-dev [platform:ubuntu]
locales [platform:debian]
mysql [platform:rpm !platform:redhat]
mysql-client [platform:dpkg !platform:debian]
mysql-devel [platform:rpm !platform:redhat test]
mysql-server [!platform:redhat !platform:debian]
mariadb-devel [platform:rpm platform:redhat test]
mariadb-server [platform:rpm platform:redhat platform:debian]
# Not in the test profile: mysqlclient is a runtime dependency, and from
# 2.2.0 it locates libmysqlclient with pkg-config rather than the
# mysql_config binary it used before. It ships no wheel, so every install
# builds it from source and needs pkg-config present. The .pc files
# themselves come from libmariadb-dev above.
pkg-config [platform:dpkg]
pkgconfig [platform:rpm]
# mysqlclient compiles a C extension, so the Python headers are needed
# too. Verified 2026-08-22: without these, pip install kerbside gets
# past pkg-config and then fails in the compile on both debian:trixie
# and rockylinux:10.
python3-dev [platform:dpkg]
python3-devel [platform:rpm]
libxml2-dev [platform:dpkg]
libxslt1-dev [platform:dpkg]
build-essential [platform:dpkg]
